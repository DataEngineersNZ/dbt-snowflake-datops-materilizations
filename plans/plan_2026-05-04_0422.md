---
created: 2026-05-04T04:22:01.713Z
session: e70654ef-b737-4ab1-ae3f-0834f1487f5c
working_directory: C:\git\dataengineers\dbt-snowflake-datops-materilizations
---


## README Update Plan

### Context

The current README (676 lines) has good coverage of most materializations but has several gaps and outdated information. The goal is to make it comprehensive enough that Cortex Code (or any AI assistant) can use it as a reference to create models for any materialization in the package.

**What's outdated:**
- Version shown as `0.2.12`, should be `0.2.13`
- `require-dbt-version` shown as `>=1.8.0, <2.0.0`, now `>=1.3.0, <3.0.0`
- No mention of dbt Fusion compatibility

**What's missing entirely:**
- Fusion compatibility section (how custom configs work with `meta`, the `config_meta_get` helper)
- Complete `dbt_project.yml` setup example showing all hooks, dispatch config, and vars in one place
- Quick reference / cheat sheet for all materializations (for AI assistants to quickly identify which to use)
- `on-run-end` hook documentation for `enable_tasks()` and `enable_alerts()` (mentioned in passing but not as a proper setup section)
- Source tables with external locations / Snowpipe (the external build plan is complex and undocumented)
- Stream configuration for external streams (`source_type = 'external'`)

**What needs fixing:**
- The `External Access Integration` config table has broken markdown (line 456 has pipe-formatting issues)
- Examples use top-level config keys but should show both Core and Fusion patterns

### Implementation Steps

**Step 1: Update header section**
- Fix version to `0.2.13`
- Fix `require-dbt-version` to `>=1.3.0, <3.0.0`
- Add dbt Fusion compatibility badge/note

**Step 2: Add "Quick Start / Setup" section after Installation**
A single, complete `dbt_project.yml` example showing:
- `dispatch` config for comment overrides
- All `on-run-start` hooks in correct order (file_formats, stages, table_sources)
- All `on-run-end` hooks (enable_tasks, enable_alerts, enable_monitorial_monitors)
- Common `vars` block

**Step 3: Add "dbt Fusion Compatibility" section**
- Explain that this package supports both dbt Core and dbt Fusion
- Document the `config_meta_get` / `config_meta_require` helpers
- Show before/after examples: Core style vs Fusion style config blocks
- Note that both styles are supported transparently

**Step 4: Add "Quick Reference" table**
A single table listing every materialization with: name, what it creates, key required configs, and which section to read. This is the most valuable section for an AI assistant trying to determine which materialization to use.

**Step 5: Add "Hooks Setup" section**
Document all 5 hook macros as a standalone section:
- `stage_file_formats()` — on-run-start
- `stage_stages()` — on-run-start
- `stage_table_sources()` — on-run-start
- `enable_tasks()` — on-run-end
- `enable_alerts()` — on-run-end
- `enable_monitorial_monitors()` — on-run-end

With parameter tables and execution order.

**Step 6: Fix the External Access Integration broken table**
Line 456 has mangled markdown — fix the `role_for_creation` row.

**Step 7: Add external source tables + Snowpipe documentation**
The `Tables` section only covers basic auto-create. Add documentation for:
- External tables with `location` property
- Snowpipe auto-creation (`snowpipe` property in external config)
- `ext_full_refresh` var for forcing external table recreation
- `retain_previous_version_flg` and `migrate_data_over_flg` options

**Step 8: Add Stream external type documentation**
The Streams section is missing `source_type = 'external'` and `source_database_prefix` usage.

### Verification
- Visually review the README renders correctly in GitHub markdown preview
- Confirm every materialization has a section with: usage example, config table, and at least one concrete model example
- Confirm the quick reference table lists all 15 materializations
- Confirm the setup section is copy-pasteable into a real `dbt_project.yml`

### Critical Files
- `README.md` — The only file being modified
- `macros/helpers/config_meta_get.sql` — Reference for documenting the Fusion compatibility helpers
- `macros/tasks/snowflake__task.sql` — Reference for config options to document
- `macros/source_tables/snowflake/snowflake__get_external_build_plan.sql` — Reference for external table/Snowpipe features to document
- `dbt_project.yml` — Reference for current version constraint


---

## User Context

I have updated the version to 1.0.0 from 0.2.13. also update the change log with the latest set of changes