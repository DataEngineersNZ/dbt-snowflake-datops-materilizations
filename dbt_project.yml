name: 'dbt_dataengineers_materilizations'
version: '0.1.0'
config-version: 2

require-dbt-version: [">=1.3.0", "<2.0.0"]

macro-paths: ["macros"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets: ["target", "dbt_modules", "dbt_packages"]        # directories to be removed by `dbt clean`
