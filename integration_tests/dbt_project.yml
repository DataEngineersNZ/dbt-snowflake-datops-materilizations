name: 'dbt_integration_tests'
version: '1.0.0'
config-version: 2

profile: 'dbt_integration_tests'

require-dbt-version: [">=1.3.0", "<3.0.0"]

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target"]

dispatch:
  - macro_namespace: dbt
    search_order: [dbt_dataengineers_materializations, dbt]

vars:
  default_monitorial_error_integration: ""
